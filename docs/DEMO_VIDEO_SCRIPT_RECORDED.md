# UndoLang demo — recorded-cut voiceover script

This is the read-aloud script matched to the attached recording:
`/Users/macbook/Movies/CapCut/Undolang.mov`.

The recording is approximately 4:56.4. The timecodes below were checked against the visible cut, including the terminal output. The original rough marker for the terminal section was early: the help screens begin at about **02:18**, not 01:38.

Read only the **Voiceover** column. Text in brackets is an editor cue and is not spoken. An em dash means to leave a small breath while the screen settles. If a command output takes a moment to appear, let the output remain visible before continuing.

| Time | What is on screen | Voiceover |
|---|---|---|
| 00:00–00:08 | Landing hero: “Filesystem changes should either finish or recover.” | “File changes are deceptively risky. A release can copy a new binary, update a configuration file, and then fail halfway through. The application is left in a partial state, and the person fixing it has to guess what happened.” |
| 00:08–00:18 | Facts row and “Readable intent. Explicit boundaries.” | “UndoLang is built for that gap: a small, readable filesystem language that plans changes, records durable intent, verifies the result, and rolls back known failures. It is a native Go 1.27 binary with no daemon, database, package runtime, or cloud account.” |
| 00:18–00:22 | Protocol strip and transition to documentation | “The promise is simple: plan before, lock and journal during the change, verify after, and recover when something goes wrong.” |
| 00:22–00:34 | Getting Started and installation pages | “The documentation starts with installation. On macOS and Linux, the local install script copies the binary into a user-owned location. On Windows, the PowerShell installer does the same. The first step is always to choose a transaction root, keep the script location independent, and run a check before changing anything.” |
| 00:34–00:42 | Language, Programs, and Transactions pages | “The language reference explains paths, strings, conditions, and the program model. One file can contain several named transactions. Running the file uses source order; selecting a name with `--transaction` runs only that transaction.” |
| 00:42–00:52 | Paths and capabilities pages | “The security pages make the boundary explicit. Relative paths stay inside the root, absolute paths require an allowed capability, `.undo` state is reserved, and symlink traversal is never treated as a shortcut around the policy.” |
| 00:52–01:04 | Agents page, workflow, Copy Context / Download Context | “UndoLang also has a machine contract for agents. An agent can inspect capabilities, validate a file, request a structured plan, and ask for approval before mutation. This button copies the complete context pack, and this download saves the same guidance as `agent-context.txt`, so another agent can understand the syntax and safety rules without guessing.” |
| 01:04–01:17 | IDE repository tree: packages and implementation layout | “Now we move into the implementation. The repository is split by responsibility: lexer and parser for the language, validation for semantics, path capabilities for safe access, a planner for effects, journal and state for durable authority, and transaction and recovery packages for execution.” |
| 01:17–01:29 | Lexer and parser files | “The lexer tracks byte offsets plus line and column, handles UTF-8, comments, quoted and raw strings, and reports stable diagnostics. The recursive-descent parser turns that source into an ordered program of named transactions.” |
| 01:29–01:40 | AST / example syntax overview | “The semantic pass then enforces the important shape of a transaction: requirements first, mutations in the middle, and assertions last. That ordering keeps preconditions, intent, and postconditions visible to both a human reviewer and the planner.” |
| 01:40–01:55 | Top of `fs.undo`, first transaction | “This is the example file used in the live test. The first transaction seeds a small fixture: it creates an input directory, writes the version-one application file, creates a configuration file, and makes a read-only directory. The `require` lines describe what must already be true.” |
| 01:55–02:08 | Operations and assertions in the file | “The mutation block contains the real filesystem verbs: `mkdir`, `write`, `copy`, `move`, `replace`, and `delete`. The `assert` lines are postconditions. If an operation fails, or an assertion is false, UndoLang rolls back that transaction’s applied work.” |
| 02:08–02:18 | Later transaction definitions, including failure cases | “The rest of the file demonstrates the boundaries: a committed transaction can stay committed, a later precondition failure stops the remaining entries, and an intentional postcondition failure exercises rollback. The file also defines missing-file and permission cases without hiding them behind fake success.” |
| 02:18–02:20 | `undo --help` | “After installation, the command-line surface is deliberately small. The top-level help lists checking, planning, running, recovery, history, inspection, capabilities, schema, and the agent guide.” |
| 02:20–02:22 | `undo run --help` | “The run help shows the important controls: a script file, an optional root, repeatable allowed paths, a selected transaction, JSON output, and explicit `--yes` approval for noninteractive mutation.” |
| 02:22–02:30 | `seed-fixture` plan, approval, committed output | “First I run `seed-fixture`. The plan shows exactly which directories and files will be created, then the program asks for approval. The transaction commits, and the explorer immediately shows the fixture it created.” |
| 02:30–02:38 | `first-commits` plan, approval, marker file | “Next is `first-commits`. It writes a marker file and verifies its contents. This is intentionally simple: it proves that a successful transaction remains visible when the next transaction has a problem.” |
| 02:38–02:46 | `second-fails` and `E_PRECONDITION_FAILED` | “Here the required source file does not exist. The runtime fails during the precondition gate with `E_PRECONDITION_FAILED`. No unreachable write is performed, and the earlier committed marker is left alone.” |
| 02:46–03:16 | `upgrade-files`: copy, replace, write `VERSION`, assertions | “Now I run `upgrade-files`. It copies the version-one application into a new path, replaces the configuration schema from version one to version two, writes a version marker, and asserts all three results. The plan names each effect before the approval prompt, and the transaction commits only after verification.” |
| 03:16–03:22 | `overwrite-current` | “This transaction demonstrates explicit overwrite. The existing destination is backed up as part of the transaction, the new current file is written, and the assertion checks the resulting contents. Overwrite is an intentional, visible effect—not an accidental directory merge.” |
| 03:22–03:44 | `retire-old-layout`: archive, move, delete | “The retirement transaction creates an archive directory, moves the current file into it, and deletes the old version-one input. The plan includes the rollback estimate, and the final assertions confirm that the archived file exists while the old locations no longer do.” |
| 03:44–04:26 | `demonstrate-rollback`, postcondition error, restored file / second attempt | “Finally, this transaction is designed to fail after its write. The plan is accepted, the temporary configuration change is applied, and the deliberately false assertion returns `E_POSTCONDITION_FAILED`. UndoLang then uses the recorded inverse to restore the previous file. That is the key distinction: a failure is not silently ignored, and a rollback is not just a message—it is a verified filesystem result.” |
| 04:26–04:28 | Transition back to terminal | “The successful transactions remain committed, while the failed transaction leaves the root in its prior state.” |
| 04:28–04:34 | `make reproducible-build` and two hashes | “For the reproducible-build bonus, I run one Make target. It performs two canonical Go 1.27 builds with CGO disabled, hashes both binaries, and prints the same SHA-256 for each. The final line is an actual byte-identical pass.” |
| 04:34–04:45 | `STDLIB.md`, then `go.mod` | “The standard-library report shows the substitutions behind the zero-dependency design: handwritten parsing and validation, `os.Root` path confinement, streaming I/O, SHA-256 and CRC32C, UUIDv7, JSON, and explicit journal state. The module file contains only the module path and `go 1.27`—there is no `require` block.” |
| 04:45–04:56 | Closing banner | “UndoLang makes filesystem intent readable, bounded, and recoverable. When a file operation matters, plan it, verify it, and keep a path back. Use UndoLang for performing safe file operations.” |

## Recording notes

- The language and implementation sections are intentionally paced with short pauses because the editor holds on source files while scrolling.
- Do not narrate the `missing-source` or `permission-denied` definitions as executed transactions; they are visible in the example source, but this recording does not show their terminal runs.
- Keep the error names exactly as displayed: `E_PRECONDITION_FAILED` and `E_POSTCONDITION_FAILED`.
- The closing line is written to match the banner exactly: **“Use UndoLang for performing safe file operations.”**
